// This is a basic service worker to make your app installable.
// You can add more caching logic here later.
self.addEventListener("fetch", (event) => {
  // For now, we are just passing through the requests.
});
